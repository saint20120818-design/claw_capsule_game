# 動物扭蛋夾夾樂

## 執行方式
直接用瀏覽器開啟 `index.html` 即可。

## 遊戲流程
1. 點擊「開始夾取」。
2. 爪子移到中央並向下夾取扭蛋。
3. 顯示隨機動物。
4. 底部對應的灰色動物圖示會亮起。
5. 收集完 10 隻動物後顯示完成畫面。

## 可調整項目
在 `index.html` 中修改 `animals` 陣列，可以更換動物名稱與 Emoji。
`chooseAnimal()` 內的 `0.7` 是優先抽到未收集動物的機率。
